name: OSSF Scorecard
on:
  push:
    branches:
      - 'v*.x/staging'
  pull_request:
    branches: 
      - 'v*.x/staging'
      - 'v*.x/rc'
      - 'v*.x/master'
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:
  schedule:
    - cron:  '30 1 * * 6'

permissions: read-all

jobs:
  analysis:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      security-events: write
      id-token: write

    steps:
      - name: "Checkout code"
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: "Run analysis"
        uses: ossf/scorecard-action@v2.3.0
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: false

      - name: "Filter analysis results to enabled checks"
        env:
          SCORECARD_ENABLED_CHECKS: TokenPermissionsID,PinnedDependenciesID,BranchProtectionID,ContributorsID,DangerousWorkflowID
        run: | 
          ENABLED_CHECKS_IN_JSON=$(echo $SCORECARD_ENABLED_CHECKS | jq -cR 'split(",")')
          FILTERED=$(cat results.sarif | jq '.runs[].results |= map(select(.ruleId as $id | '"$ENABLED_CHECKS_IN_JSON"' | index($id)))')
          if [ "$(echo "$FILTERED" | jq '.runs[].results | length')" -eq 0 ]; then
            jq '.runs[].results = []' results.sarif > filteredResults.sarif
          else
            echo "$FILTERED" > filteredResults.sarif
          fi
          
      - name: "Generate summary table for Scorecard checks"
        env:
          SCORECARD_ENABLED_CHECKS: TokenPermissionsID,PinnedDependenciesID,BranchProtectionID,ContributorsID,DangerousWorkflowID
        run: |
          echo "| Check ID | Findings |" > scorecard-summary.md
          echo "|----------|----------|" >> scorecard-summary.md
          for id in $(echo $SCORECARD_ENABLED_CHECKS | tr ',' ' '); do
            count=$(jq --arg id "$id" '[.runs[].results[] | select(.ruleId == $id)] | length' filteredResults.sarif)
            echo "| $id | $count |" >> scorecard-summary.md
          done
          echo "" >> scorecard-summary.md
          echo "See full SARIF for details." >> scorecard-summary.md
          cat scorecard-summary.md >> $GITHUB_STEP_SUMMARY

      - name: "Upload artifact"
        uses: actions/upload-artifact@v4
        with:
          name: SARIF file
          path: |
            results.sarif
            filteredResults.sarif
          retention-days: 3


      - name: "Upload to code-scanning"
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: filteredResults.sarif
